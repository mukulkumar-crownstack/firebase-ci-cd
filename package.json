{
  "name": "@pr-ui/firestore",
  "description": "Firebase Functions package, auto generated by @simondotm/nx-firebase",
  "scripts": {
    "lint": "cd ../../ && nx lint firestore",
    "build": "cd ../../ && nx build firestore --with-deps",
    "serve": "npm run build && firebase emulators:start --only functions --config ../../firebase.firestore.json",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions --config ../../firebase.firestore.json",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "16"
  },
  "main": "src/index.js",
  "dependencies": {
  },
  "private": true
}
